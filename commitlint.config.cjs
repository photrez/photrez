// Commit message linting (Conventional Commits) — the same standard used by
// Electron, Vite and Angular. Enforced locally via the commit-msg husky hook
// and by CI. See https://www.conventionalcommits.org.
module.exports = {
  extends: ['@commitlint/config-conventional'],
};
